# ════════════════════════════════════════════════════════════════
# TestForge - Variables de Entorno
# ════════════════════════════════════════════════════════════════
#
# INSTRUCCIONES:
# 1. Copia este archivo como: .env.local
#    $ cp .env.local.example .env.local
#
# 2. Reemplaza los valores de ejemplo con los reales
#
# 3. NUNCA hagas commit de .env.local (está en .gitignore)
#
# 4. Reinicia el servidor después de cambiar variables:
#    $ npm run dev
#
# ════════════════════════════════════════════════════════════════

# ────────────────────────────────────────────────────────────────
# Configuración General
# ────────────────────────────────────────────────────────────────

NODE_ENV=development

# URL base del sitio (sin trailing slash)
# - Desarrollo: http://localhost:3000
# - Producción: https://testforge.com
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# ────────────────────────────────────────────────────────────────
# Supabase (Base de datos y autenticación)
# ────────────────────────────────────────────────────────────────
# Obtén estos valores en:
# https://supabase.com/dashboard/project/TU_PROYECTO/settings/api
#
# IMPORTANTE:
# - ANON_KEY: Segura para cliente (operaciones públicas)
# - SERVICE_ROLE_KEY: SOLO servidor (acceso admin total)

NEXT_PUBLIC_SUPABASE_URL=https://tu-proyecto.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.tu-anon-key-aqui
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.tu-service-role-key-aqui

# ────────────────────────────────────────────────────────────────
# Stripe (Pagos)
# ────────────────────────────────────────────────────────────────
# Obtén estos valores en:
# https://dashboard.stripe.com/apikeys
#
# IMPORTANTE:
# - Usa keys de TEST (sk_test_, pk_test_) para desarrollo
# - Usa keys de LIVE (sk_live_, pk_live_) SOLO en producción
# - NUNCA expongas la SECRET_KEY al cliente

# Clave pública (segura para cliente)
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_tu-publishable-key-aqui

# Clave secreta (SOLO servidor)
STRIPE_SECRET_KEY=sk_test_tu-secret-key-aqui

# Secreto de webhook (para verificar eventos de Stripe)
# Obtén esto al crear un webhook en: https://dashboard.stripe.com/webhooks
STRIPE_WEBHOOK_SECRET=whsec_tu-webhook-secret-aqui

# ────────────────────────────────────────────────────────────────
# Resend (Emails transaccionales)
# ────────────────────────────────────────────────────────────────
# Obtén la API key en:
# https://resend.com/api-keys
#
# Resend se usa para:
# - Confirmación de compra
# - Envío de datasets por email
# - Notificaciones

RESEND_API_KEY=re_tu-api-key-aqui

# ────────────────────────────────────────────────────────────────
# AI Providers (Opcional - Fase 3: Generador de Tests)
# ────────────────────────────────────────────────────────────────
# Estas variables son OPCIONALES y solo se necesitan para la
# funcionalidad de generación automática de tests.
#
# Puedes configurar una, ambas, o ninguna.

# OpenAI (GPT-4, etc.)
# https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-tu-openai-key-aqui

# Anthropic (Claude)
# https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=sk-ant-tu-anthropic-key-aqui

# ════════════════════════════════════════════════════════════════
# NOTAS DE SEGURIDAD
# ════════════════════════════════════════════════════════════════
#
# 1. NUNCA hagas commit de .env.local a Git
#
# 2. Variables NEXT_PUBLIC_* se exponen al cliente (browser)
#    - Solo pon aquí valores que sean seguros de exponer
#
# 3. Todas las demás variables son SOLO para el servidor
#    - Si las expones al cliente, comprometes tu seguridad
#
# 4. En producción, configura estas variables en tu hosting:
#    - Vercel: https://vercel.com/docs/environment-variables
#    - Railway: https://docs.railway.app/develop/variables
#
# ════════════════════════════════════════════════════════════════
